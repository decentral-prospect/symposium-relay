name: CI

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
  workflow_dispatch:

# CI never needs to write to the repository.
permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    # Keep this name in sync with the required status check in the main ruleset.
    name: Test and build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    env:
      # github.com/mattn/go-sqlite3 requires CGO.
      CGO_ENABLED: "1"

      # Do not silently download a different Go toolchain.
      # setup-go installs the version declared in go.mod.
      GOTOOLCHAIN: local

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Go
        uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Check formatting
        shell: bash
        run: |
          files="$(gofmt -l .)"

          if [[ -n "$files" ]]; then
            echo "The following files are not formatted with gofmt:"
            printf '%s\n' "$files"
            exit 1
          fi

      - name: Verify module metadata
        run: |
          go mod tidy -diff
          go mod verify

      - name: Run go vet
        run: |
          go vet -mod=readonly ./...

      - name: Run tests
        run: |
          go test \
            -mod=readonly \
            -vet=off \
            -count=1 \
            -timeout=10m \
            ./...

      - name: Check development build
        run: |
          mkdir -p "$RUNNER_TEMP/build"

          go build \
            -mod=readonly \
            -trimpath \
            -buildvcs=true \
            -o "$RUNNER_TEMP/build/symposium-server-development" \
            .

          file "$RUNNER_TEMP/build/symposium-server-development"
